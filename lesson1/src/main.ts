let username = "Dave";
console.log(username);

//Explicitly typed but TypeScript does not stop it from running because the config file is not set up to stop.
let a: number = 12;
let b: string = "6";
let c: number = 2;

console.log(a / b);
console.log(c * b);
